# LeafAI Frontend Client

Modern React + TypeScript + Vite + Tailwind CSS web application for plant disease diagnosis.

## Available Scripts

- `npm run dev`: Start Vite development server at http://localhost:3000.
- `npm run build`: Type-check with `tsc` and create production bundle in `dist/`.
